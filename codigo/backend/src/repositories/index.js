const usersRepository = require('./user.repository')
const devFieldRepository = require('./devfield.respository')
const clientRepository = require('./client.repository')
const devRepository = require('./dev.repository')

module.exports = {
  usersRepository,
  devFieldRepository,
  devRepository,
  clientRepository
}
